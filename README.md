# Analisis-de-Datos

* Para este Parcial 2 se utiliza originalmente el archivo transferencias_autos.csv
* Luego se guarda para un futuro uso en transferencias_autos_limpio.csv
* Y por ultimo se guarda una version pequeña que solo utiliza el 10% de los datos para utilizar en Grafana (se guardo de una forma especifica para que tenga transferencias al azar de todos los dias)
